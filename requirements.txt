# ═══════════════════════════════════════════════════════════════
# requirements.txt — YouTube Live Transcribe (Voxtral Mini)
# ═══════════════════════════════════════════════════════════════
# Tested on: Python 3.11.14, CUDA 12.6, Ubuntu 22.04/24.04
#
# BEFORE running pip install, make sure these system packages
# are installed:
#
#   sudo apt update
#   sudo apt install ffmpeg git curl
#
# Then install Python dependencies:
#   pip install -r requirements.txt
#
# ═══════════════════════════════════════════════════════════════

# ── PyTorch with CUDA 13.0 ──────────────────────────────────
# PyTorch 2.11 crashes in torch_cpu.dll while materializing Voxtral on
# Windows. The 2.13 CUDA wheel loads the complete model successfully.
--extra-index-url https://download.pytorch.org/whl/test/cu130
torch==2.13.0+cu130
# 2.13 TorchAudio Windows wheels are not published yet. The application
# passes in-memory audio to pyannote and does not depend on TorchAudio ABI.
torchaudio==2.11.0+cu130

# ── Voxtral model (Mistral) ──────────────────────────────────
transformers==5.3.0
mistral_common==1.9.1
accelerate==1.13.0
huggingface_hub==1.6.0
safetensors==0.7.0
sentencepiece==0.2.1

# ── Web server (Flask UI) ────────────────────────────────────
Flask==3.1.3
Werkzeug>=3.1.0

# ── Local diarization (open-source, offline-capable) ─────────
pyannote.audio==4.0.7

# ── YouTube stream download ──────────────────────────────────
# Includes yt-dlp-ejs, required for current YouTube JavaScript challenges.
yt-dlp[default]==2026.7.4

# ── Audio processing ─────────────────────────────────────────
numpy==2.4.3
soundfile==0.13.1

# ── Optional stable-broadcast transcription backend ─────────
# Provides word timestamps for precise pyannote speaker/text fusion.
faster-whisper==1.2.1
nvidia-cublas-cu12==12.9.2.10; sys_platform == "win32"

# ── Utilities ────────────────────────────────────────────────
requests>=2.28.0

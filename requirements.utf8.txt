# ═══════════════════════════════════════════════════════════════
# requirements.txt — YouTube Live Transcribe (Voxtral Mini)
# ═══════════════════════════════════════════════════════════════
# Tested on: Python 3.11.14, CUDA 12.6, Ubuntu 22.04/24.04
#
# BEFORE running pip install, make sure these system packages
# are installed:
#
#   sudo apt update
#   sudo apt install ffmpeg git curl
#
# Then install Python dependencies:
#   pip install -r requirements.txt
#
# ═══════════════════════════════════════════════════════════════

# ── PyTorch with CUDA 12.6 ──────────────────────────────────
# If you don't have a CUDA GPU, remove the --extra-index-url
# line and change torch==2.7.1 to just: torch
torch
torchaudio

# ── Voxtral model (Mistral) ──────────────────────────────────
transformers==5.3.0
mistral_common==1.9.1
accelerate==1.13.0
huggingface_hub==1.6.0
safetensors==0.7.0
sentencepiece==0.2.1

# ── Web server (Flask UI) ────────────────────────────────────
Flask==3.1.3
Werkzeug>=3.1.0

# ── Local diarization (open-source, offline-capable) ─────────
pyannote.audio==4.0.7

# ── YouTube stream download ──────────────────────────────────
yt-dlp==2026.3.3

# ── Audio processing ─────────────────────────────────────────
numpy==2.4.3
soundfile==0.13.1

# ── Utilities ────────────────────────────────────────────────
requests>=2.28.0
